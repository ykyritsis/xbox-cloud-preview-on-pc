{
  "name": "xbox-preview-ui",
  "version": "0.26.0-beta.3",
  "private": true,
  "description": "A controller-first Windows game launcher inspired by the Xbox cloud interface.",
  "main": "src/main.cjs",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "dist": "electron-builder --win nsis",
    "dist:portable": "electron-builder --win portable"
  },
  "devDependencies": {
    "electron": "^39.2.7",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.nebula.pclauncher",
    "productName": "Xbox Preview UI",
    "files": [
      "src/**/*",
      "build/icon.svg",
      "build/icon.png",
      "package.json"
    ],
    "win": {
      "target": "nsis",
      "icon": "build/icon.png",
      "artifactName": "Xbox-Preview-UI-Setup-${version}.${ext}"
    },
    "portable": {
      "artifactName": "Xbox-Preview-UI-Portable-${version}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
